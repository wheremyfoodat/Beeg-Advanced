use crate::ARM::arm;
use crate::bus::Bus;
use crate::cpu::CPU;
use crate::cpu::CPUModes;

#[macro_use]
use crate::isBitSet;

impl CPU {
    pub fn handleDataProcessing(&mut self, bus: &Bus, instruction: u32) {

    }
}